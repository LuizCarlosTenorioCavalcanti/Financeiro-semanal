# Instruções para o Assistente — Dashboard Grupo Delta

Este documento existe para que qualquer conta/sessão do Claude que abrir esta pasta consiga continuar o trabalho exatamente como vinha sendo feito, mesmo trocando de usuário, computador ou reiniciando a memória. Leia isto antes de mexer em qualquer arquivo `Dashboard Grupo Delta v5.4.html` ou `index.txt`.

## Contexto geral

Luiz Carlos é supervisor administrativo no Grupo Delta Construtora (João Pessoa/PB). Este projeto mantém um dashboard financeiro único em HTML, reunindo **8 módulos** em abas: Daily Briefing, Financeiro A Pagar, Financeiro A Receber, Financeiro Global Mensal (Análise Mensal), Contratos, Conciliação Bancária, Comissão e Vendas.

> O módulo "Descontos nas Unidades" que existia nas versões antigas (v3.1–v4.x) foi **descontinuado** e não faz mais parte do dashboard atual — não procurar `Dashboard_Descontos_*.html` nem reintroduzir essa aba sem pedido explícito do Luiz.

## Arquivos principais

- **`Dashboard Grupo Delta v5.4.html`** — versão OFICIAL de produção atual.
- **`index.txt`** — mesma versão oficial, mantido em sincronia byte-a-byte com o v5.4.html (é o arquivo publicado/acessado pelos usuários finais).
- **`Dashboard Grupo Delta v5.3.html`** e anteriores — OBSOLETOS. Nunca editar; servem só de histórico/backup.

Todo trabalho de dados e correções acontece em **v5.4.html e index.txt em paralelo** (sempre os dois, nessa ordem ou junto — qualquer edição feita em um precisa ser replicada identicamente no outro antes de considerar a tarefa concluída).

### Módulos atuais e arquivos-fonte

| id (interno) | Aba / sublabel | Ícone | Arquivo-fonte | Versão atual embutida |
|---|---|---|---|---|
| `briefing` | Daily Briefing / Resumo do Dia | ⚡ | não tem arquivo-fonte próprio — é montado a partir dos dados dos outros módulos já embutidos no próprio v5.4 | v1.1 |
| `financeiro` | Financeiro / A Pagar | ↓ | `Dashboard Financeiro v3.3.html` | v3.3 |
| `areceber` | Financeiro / A Receber | ↑ | `Dashboard A Receber v2.3.html` | v2.3 |
| `analisemensal` | Financeiro / Global Mensal | $ | `Dashboard Analise Mensal v2.1.html` | v2.1 |
| `contratos` | Contratos / Gestão | ≡ | `Dashboard Contratos v3.5.html` | v3.5 |
| `conciliacao` | Conciliação / Balanço | ⇌ | `Dashboard_Conciliacao_v2.4.html` | v2.4 |
| `comissoes` | Comissão / Unidades | % | `Dashboard_Comissoes_v1.2.html` | v1.2 |
| `vendas` | Vendas / Unidades | ⌂ | `Dashboard_Vendas_v1.X.html` | v1.1 |

Cada um desses arquivos-fonte é standalone (a maioria também no formato `__bundler`, ver abaixo) e o Luiz substitui na pasta quando tem dados novos. **Antes de confiar num nome de versão, confira sempre o `TAB_METAS` de dentro do v5.4.html — é a fonte de verdade, não esta tabela** (esta tabela é só um retrato do momento da última atualização deste documento).

## Fluxo padrão: "atualiza os dados"

**Regra combinada com o usuário**: sempre que ele pedir "atualiza os dados", **primeiro liste todos os módulos com data/hora de cada arquivo-fonte, compare com o que já está no v5.4, e aguarde confirmação explícita antes de aplicar qualquer coisa.** Nunca aplique direto sem esse passo. Exceção já validada: se o Luiz mandar o arquivo junto com um pedido direto tipo "verifica e atualiza"/"ATUALIZAR", o diff mecânico (bug conhecido, merge simples) pode ser aplicado no mesmo turno — mas qualquer coisa que não seja mecânica (mudança real de dado, valor ambíguo de fonte, campo que não bate) ainda precisa ser listada e esperar confirmação antes de aplicar.

### Checklist obrigatório (não pular nenhum item — item 6 já foi esquecido uma vez)

1. Rodar `ls -la --time-style=full-iso *.html` na pasta e comparar mtimes com as datas salvas no `TAB_METAS` do v5.4 atual.
2. **Cuidado com mount desatualizado**: às vezes o acesso à pasta fica "preso" num retrato antigo (mtimes não batem com o que o usuário vê no Explorer, ou um arquivo recém-staged continua voltando com o conteúdo antigo). Se suspeitar disso, avise o usuário — e tente o truque de **copiar o arquivo com outro nome no device_bash e dar stage nessa cópia** (`cp "X.html" "X_fresh.html"` na pasta do usuário, depois `device_stage_files` no `_fresh.html`) — isso força um caminho novo e furou o cache quando um `device_stage_files` repetido no mesmo nome não atualizava.
3. **Verificar se o arquivo-fonte não está truncado/corrompido**: já aconteceu MAIS DE UMA VEZ (Contratos e Conciliação) do arquivo salvo pelo usuário terminar no meio do código, sem fechar `</script></body></html>`. Sempre checar `'</html>' in conteudo` antes de confiar no arquivo.
4. Para cada módulo que parece mais novo, **verificar o conteúdo de verdade** (não só a data): comparar os dados byte a byte / campo a campo entre o arquivo-fonte e o que já está embutido no v5.4. A data/hora exibida na tela é só texto, pode estar errada — não confiar nela isoladamente. **Use os scripts prontos** (desde 24/07/2026: `update_contratos.py`, `update_vendas.py`, `update_financeiro.py`, `update_conciliacao.py` — ou simplesmente `atualizar_tudo.py`, que acha e roda os 4 sozinho — ver seção própria abaixo) em vez de escrever o diff na mão; eles já embutem as chaves corretas, o bug conhecido dos `dias_*` e as fórmulas do Daily Briefing.
5. Listar para o usuário o que precisa atualizar (mudanças reais, não as de bug conhecido) e aguardar "pode seguir" (ou similar) antes de aplicar qualquer coisa ambígua.
6. **Checar se a mudança afeta o Daily Briefing** — sempre, mesmo que o pedido tenha sido só "atualiza [outro módulo]". Ver seção "Fórmulas do Daily Briefing" abaixo. Isso já foi esquecido uma vez nesta sessão ("erro grande, você não atualizou o daily") — não repetir.
7. Aplicar as atualizações (ver técnica de transplante abaixo, ou rodar o script com `--apply`).
8. Atualizar `TAB_METAS` (data de cada módulo) e o rodapé do sidebar (`vX.X · Atualizado DD/MM/AAAA às HH:MM`, hora de Brasília/sincronização, não a hora do dado em si).
9. Validar: reabrir o JSON (`json.loads`) dos dois arquivos e confirmar que parseia sem erro. Depois, rodar `python3 scripts/validate_dashboard.py` (desde 24/07/2026 à noite — antes disso era um script Playwright escrito na mão toda vez) e conferir "0 erros" nos dois arquivos. Ele já cuida de: copiar `index.txt` pra um `.html` temporário (Chromium não executa `<script>` num arquivo servido como `.txt`), entrar a senha do gate (`deltabi`), clicar nas 8 abas com cache-busting. **Importante sobre os cartões (principalmente Vendas)**: eles têm uma animação de contagem de ~1-2s ao renderizar — se for tirar print manual pra conferir um valor (em vez de rodar o script), espere pelo menos 2s depois de trocar de aba, senão o número capturado é intermediário da animação, não o final (já pareceu um bug de cálculo errado que na real não era nada — o valor final sempre bateu).
10. Empacotar os dois arquivos num `.zip`, entregar com `SendUserFile`, mandar pro `device_commit_files`, extrair no `device_bash` do lado do usuário e **conferir md5sum** dos arquivos finais contra os que ficaram no workspace — só then considerar a entrega concluída (ver "Entrega de arquivos grandes" abaixo).
11. **Commitar no git local do Luiz** (combinado em 30/07/2026 — ver seção "Commit automático no git local" abaixo): depois da entrega confirmada por md5sum, rodar `git add` (só nos arquivos relevantes ao dashboard, nunca `-A`/`git add .`) + `git commit` via `device_bash` na pasta do Luiz. **NUNCA dar `git push`** — isso fica sempre por conta dele (ver motivo na seção própria).

## Scripts prontos (evitam reescrever a lógica do zero a cada sessão)

Na pasta tem uma subpasta `scripts/` com:
- `bundler_utils.py` — funções compartilhadas: `unpack`/`repack`/`find_matching`/`extract_js_object`/`check_not_truncated` (originais) e, desde 24/07/2026 à noite, também `load_source_text` (lê arquivo-fonte que pode vir `__bundler` ou HTML puro, sem precisar checar isso na mão toda vez), `module_bounds` (acha `(start, end)` de um módulo pelos comentários `/* ==== X module ==== */`), `extract_js_object_bounded` (igual `extract_js_object` mas exige achar a variável DENTRO de um intervalo — evita casar com a variável errada quando dois módulos usam o mesmo nome, ex: `const DATA` existe em Financeiro E em Vendas), e `patch_tab_meta` (atualiza só o campo `data:'Atualizado: ...'` de uma entrada do `TAB_METAS` por regex **delimitada ao bloco `TAB_METAS`** — nunca usar regex livre no arquivo inteiro pra isso, o mesmo texto `'chave', label:...` aparece em outro lugar do arquivo, no array de config das abas do menu, e uma regex sem essa fronteira casa errado).
- `update_contratos.py` — diff + merge do módulo Contratos: bug `dias_*`/`tempo_total`, recálculo do card do Daily Briefing, e **já grava o `TAB_METAS.contratos` sozinho no `--apply`** (não precisa mais fazer isso na mão).
- `update_vendas.py` — diff + merge do módulo Vendas (KPIs, checagem se o card fixo do Daily Briefing precisa recálculo manual), **já grava `TAB_METAS.vendas` sozinho no `--apply`**.
- `update_financeiro.py` (novo, 24/07/2026) — diff + swap do módulo Financeiro (`DATA.pagar`/`DATA.pago`). Como esses registros não têm ID único, o diff é por conteúdo inteiro do registro (multiset). Reporta valor total e por empresa dos registros novos/removidos, e se é "puramente aditivo" (sem remoção — sinal de que pode aplicar direto). **Já grava `TAB_METAS.financeiro` sozinho no `--apply`**.
- `update_conciliacao.py` (novo, 24/07/2026; alerta estendido em 30/07/2026) — diff + swap do módulo Conciliação (`RELATORIOS`/`PENDENCIAS`/`INVESTIMENTOS`). `RELATORIOS` normalmente é troca completa a cada rodada (relatório de ontem sai, o de hoje entra) — o script mostra saldo/não-conciliado por empresa, prod vs novo, e **alerta se o saldo variar mais de 20%** (já aconteceu de uma correção de conciliação mudar o saldo de -R$475mil pra +R$1,17mi — sempre vale confirmar com o Luiz que a mudança é real antes de aplicar). `PENDENCIAS` é comparada por conteúdo (lista curta, mostra o que entrou/saiu). **Já grava `TAB_METAS.conciliacao` sozinho no `--apply`**. **Bug corrigido em 30/07/2026**: o alerta de 20% só cobria o saldo BANCÁRIO (`RELATORIOS`), nunca o `INVESTIMENTOS` — um caso real passou batido (Viva Delta caiu 96%, MRDELTA subiu 360% no MESMO dia, sem saldo bancário mudar nada) e só foi pego porque conferi o print inteiro na mão antes de aplicar, não porque o script avisou. Corrigido pra checar os dois com o mesmo threshold (`SALDO_ALERTA_PCT = 0.20`) e um único bloco de alertas consolidado no fim (antes tinha dois `if alertas:` separados, um deles reimprimia os alertas de saldo de novo por engano — também corrigido). **Lição**: sempre que um script tiver um "campo opcional" tipo `INVESTIMENTOS` que é lido/comparado separado do fluxo principal, checar se ele também precisa do MESMO tratamento de alerta/validação do campo principal — não assumir que só por reportar a variação no print já é suficiente, o Luiz não vai necessariamente notar uma linha de texto no meio de uma lista se não estiver destacada como alerta.
- `validate_dashboard.py` (novo, 24/07/2026) — a validação Playwright (passo 9 do checklist) virou script: abre os dois arquivos de produção, entra a senha do `index.txt` sozinho, clica nas 8 abas, reporta erro de console se houver. Roda com `python3 scripts/validate_dashboard.py` — sem argumento nenhum, já sabe os nomes dos arquivos de produção. **Importante**: os cartões (principalmente o de Vendas) têm uma animação de contagem de ~1-2s — o script já espera o tempo certo depois de cada clique de aba; se for tirar print manual pra conferir valor, esperar pelo menos 2s depois de trocar de aba, senão pega o número no meio da animação (já aconteceu de "parecer" um bug de cálculo que na real era só isso).
- `update_comissoes.py` (novo, 28/07/2026) — diff + swap do módulo Comissão (`const DADOS`, lista sem ID único — diff por multiset igual Financeiro). Recalcula o card "Comissão — Unidades" do Daily Briefing (período = par `periodo_inicio`/`periodo_fim` mais recente presente nos dados, total/qtd desse período, quebra por `empreendimento`). **Já grava `TAB_METAS.comissoes` sozinho no `--apply`**. Ver seção própria abaixo sobre a armadilha do cache stale que gerou um falso "0 diferenças" antes deste script existir.
- `update_areceber.py` (novo, 29/07/2026) — diff + swap do módulo A Receber (`const RAW`, lista achatada sem ID único, um registro por empreendimento dentro de um "período" ano/mes/dia_ini/dia_fim — diff por multiset via JSON canônico, igual Financeiro). Recalcula o card "Financeiro — A Receber" do Daily Briefing (período = maior tupla `(ano,mes,dia_ini,dia_fim)` presente, total/qtd desse período, quebra por `empreendimento`, string `periodo` no formato "DIA_INI a DIA_FIM/MES/ANO"). **Já grava `TAB_METAS.areceber` sozinho no `--apply`**. **Armadilha própria**: o arquivo-FONTE (standalone) grava `const RAW` como literal JS com chaves de objeto SEM aspas (`{ano:2024,mes:1,...}`) — não é JSON válido, `json.loads` direto falha. Só a produção (já normalizada por um `json.dumps` anterior) tem JSON de verdade. O script usa um parser tolerante próprio (`_load_js_array`, regex `([{,]\s*)([A-Za-z_]\w*)(\s*:)` adicionando aspas nas chaves) só para ler o arquivo-fonte — vale a pena checar se outro arquivo-fonte novo (ex: Análise Mensal, se um dia ganhar script) tem o mesmo problema antes de assumir `json.loads` direto vai funcionar.
- `atualizar_tudo.py` (novo, 24/07/2026; atualizado 28/07/2026 pra incluir Comissões, 29/07/2026 pra incluir A Receber) — **o comando único pro fluxo "atualiza os dados"**: varre a pasta, acha sozinho quais dos 6 arquivos-fonte com script (Contratos/Vendas/Financeiro/A Receber/Conciliação/Comissões) foram colocados (por nome do arquivo, pega o mais recente se houver mais de um candidato), roda o diff de cada um, e com `--apply` já aplica tudo e chama o `validate_dashboard.py` no final. Módulos sem arquivo novo na pasta são simplesmente pulados (mostra "nenhum arquivo-fonte novo encontrado").

Uso típico (o fluxo novo, mais rápido):
```bash
cd "pasta do dashboard"
python3 scripts/atualizar_tudo.py                # modo consulta — mostra o diff de tudo que achar
python3 scripts/atualizar_tudo.py --apply         # aplica tudo + já valida no final
```
Ou módulo a módulo, se preferir controlar um de cada vez (sempre nessa ordem — primeiro sem `--apply`, mostrar pro Luiz, só depois `--apply`):
```bash
python3 scripts/update_contratos.py "Dashboard Contratos v3.5.html"
python3 scripts/update_contratos.py "Dashboard Contratos v3.5.html" --apply

python3 scripts/update_vendas.py "Dashboard_Vendas_v1.2.html" --apply
python3 scripts/update_financeiro.py "Dashboard Financeiro v3.3.html" --apply
python3 scripts/update_areceber.py "Dashboard A Receber v2.3.html" --apply
python3 scripts/update_conciliacao.py "Dashboard_Conciliacao_v2.4.html" --apply

python3 scripts/validate_dashboard.py             # 8 abas, zero erros = pode entregar
```
O script NÃO existe ainda só para Análise Mensal (A Receber ganhou o seu em 29/07/2026, Comissão em 28/07/2026, ver acima) — para esse, o diff ainda é manual; se for fazer um script novo, seguir o mesmo padrão (`bundler_utils.py`, principalmente `module_bounds`/`extract_js_object_bounded`/`patch_tab_meta` — não reinventar; e checar antes se o arquivo-fonte usa JSON válido ou chaves sem aspas tipo o de A Receber).

## Fórmulas do Daily Briefing — card "Contratos — Gestão"

Reverse-engineered nesta sessão (batem com os números históricos definidos à mão antes). Documentado aqui pra não ter que redescobrir:

- `contrato_sienge_total` = conta contratos com `data_contrato_sienge` preenchida E `data_assinatura` vazia.
- `contrato_sienge_novo` = conta contratos com `data_contrato_sienge` == HOJE.
- `assinados_novo` = conta contratos com `data_assinatura` == HOJE.
- `assinados_triagem_total` = conta contratos com `etapa_itbi == 'TRIAGEM'`.
- `itbi_solicitado_total` = conta contratos com `etapa_itbi == 'ITBI SOLICITADO'`.
- `itbi_solicitado_novo` = conta contratos com `data_solicitacao_itbi` == HOJE (**campo novo, criado em 28/07/2026** — mesmo padrão de bug do `entregue_agencia_novo`: a linha "ITBI Solicitado" do card tinha `novo:null` fixo no front-end E o `compute_briefing_card` nunca calculava esse valor. O Luiz percebeu porque sabia que tinha solicitado ITBI de contratos naquele dia e a coluna Novos aparecia "-").
- `itbi_solicitado_atrasados` = conta contratos com `etapa=='ITBI' E situacao_itbi=='ATRASADO'`.
- `cartorio_protocolado_total` = conta contratos com `etapa=='CARTÓRIO'`.
- `cartorio_protocolado_atrasados` = conta contratos com `etapa=='CARTÓRIO' E situacao_cartorio=='ATRASADO'`.
- `cartorio_protocolado_novo` = conta contratos com `data_entrada_cartorio` == HOJE.
- `cartorio_registrado_novo` = `extraInfo.reg_ulysses + extraInfo.reg_eunapio` (vem pronto do arquivo-fonte — se parecer estranho/inconsistente com o resto dos dados, perguntar ao Luiz antes de aplicar, já aconteceu esse valor cair pra 0 sem nenhuma mudança correspondente nos contratos de cartório).
- `entregue_agencia_total` = conta contratos com `etapa=='AGÊNCIA'` (FOTO do estado atual, mesmo padrão do Cartório Protocolado — **corrigido em 24/07/2026 à noite**, antes estava calculado errado como contagem "== HOJE", ver observação abaixo).
- `entregue_agencia_atrasados` = conta contratos com `etapa=='AGÊNCIA' E situacao_agencia=='ATRASADO'`.
- `entregue_agencia_novo` = conta contratos com `data_entregue_agencia` == HOJE (**campo novo, criado em 24/07/2026 à noite** — antes não existia, e a linha "Entregue na Agência" do card tinha `novo:null` fixo no código do front-end, nunca mostrando nada nessa coluna).
- `credito_liberado` = conta contratos com `data_credito` == HOJE.

**Regra importante (corrigida em 24/07/2026, confirmada pelo Luiz)**: todos os campos "_novo" (`contrato_sienge_novo`, `assinados_novo`, `cartorio_protocolado_novo`, `cartorio_registrado_novo`, `entregue_agencia_novo`, `itbi_solicitado_novo`, `credito_liberado`) usam **só o dia da atualização** (`extraInfo.today`), não uma janela de 2 dias. Isso é diferente do `janela_recente` do topo do `BRIEFING_DATA` (que controla se o CARD inteiro aparece na seção "recente" vs "anterior" do Daily — esse sim é uma janela de 2 dias, "hoje + dia útil anterior"). São dois conceitos diferentes, não confundir: um decide o que aparece DENTRO do card (sempre só hoje), o outro decide se o card aparece na seção de cima ou de baixo do Daily (2 dias).

Os campos "_total" e "_atrasados" (`contrato_sienge_total`, `itbi_solicitado_total`, `cartorio_protocolado_total`/`_atrasados`, `assinados_triagem_total`, `entregue_agencia_total`/`_atrasados`) são uma FOTO do estado atual (baseados em `etapa`/`situacao`) — não usam janela nenhuma, contam tudo que está naquele estado agora, independente de quando entrou nele.

**Armadilha (corrigida em 24/07/2026, RECORRENTE — aconteceu de novo em 28/07/2026 com `itbi_solicitado_novo`)**: o padrão de bug é sempre o mesmo — uma linha do card "Contratos — Gestão" tem `novo:null` FIXO no array `ctRows` do render do Daily Briefing (dentro do módulo Daily Briefing, não do Contratos), e/ou o `compute_briefing_card` (em `update_contratos.py`) nunca calcula esse campo. O Luiz sempre percebe pelo mesmo sintoma: sabe que teve movimento daquele tipo hoje, mas a coluna "Novos" aparece vazia ("-"). **Sempre que o Luiz disser algo como "teve X hoje e não apareceu"**: (1) achar a linha correspondente em `ctRows` (procurar `novo:null` hardcoded — é o sinal claro do bug) e trocar pra `novo:m.CAMPO_novo`; (2) conferir se `compute_briefing_card` realmente calcula esse `CAMPO_novo` (procurar o campo correspondente que tem uma data — ex: `data_solicitacao_itbi` pra ITBI, `data_entregue_agencia` pra Agência); se não calcular, adicionar `sum(1 for r in contratos if is_today(r.get('data_CAMPO')))`; (3) depois de corrigir, **varrer TODOS os campos `data_*` do dataset de Contratos comparando com `extraInfo.today`** (não só o que o Luiz mencionou) pra achar outros eventos do dia que talvez também não estejam sendo mostrados em lugar nenhum do card — é rápido fazer isso com Python (`Counter` de quais campos batem com a data de hoje em cada registro) e evita ter que repetir esse mesmo processo de novo na próxima vez que o Luiz notar outro campo faltando.

## Bug conhecido: `dias_*` / `tempo_total` absurdos (módulo Contratos)

O export do Sienge calcula `dias_solicitar_itbi`, `dias_entregar_cartorio`, `dias_processo_agencia`, `dias_aguardando_agencia` e `tempo_total` contra uma data padrão/época quando a data real está em branco, gerando valores tipo `46226` (~126 anos) ou negativos grandes tipo `-45000`. **Regra de correção**: para cada registro, qualquer um desses 5 campos com `abs(valor) > 1000` vira `null`. O script `update_contratos.py` já faz isso sozinho.

**NÃO reverter `situacao_cartorio` automaticamente** mesmo quando o mesmo registro foi "tocado" pelo bug acima — script já fez isso até 27/07/2026 e escondeu uma mudança REAL (achado pelo Luiz: "tem mudança no contratos", meu diff dizia "0 mudanças reais"). Caso concreto: FELIPE ALVES DE LIMA e FRANCISCO AGNO NASCIMENTO DA SILVA (MONTALCCINO) foram de `OK`→`ATRASADO` no cartório porque a `previsao_cartorio` (23/07) já tinha passado o `extraInfo.today` do próprio arquivo-fonte (24/07) — mudança real e correta. O bug dos dias_* nesses mesmos registros veio de OUTRO campo em branco (ex: `data_entregue_agencia` nulo), sem relação com o cartório. Conclusão: a correlação "registro tem bug de dias_* E situacao_cartorio mudou" NÃO implica que a mudança de situacao_cartorio seja espúria — deixar sempre aparecer como mudança real pro Luiz confirmar, nunca reverter em silêncio.

**Card do Daily Briefing pode oscilar de formato JSON entre compacto e com espaço**: a busca pelo card usa `'"id":"contratos"'` (sem espaço, formato original do arquivo), mas a própria gravação do card (`json.dumps` sem `separators=`) usava o padrão do Python que TEM espaço depois de `:`/`,` — então depois do primeiro `--apply` o trecho já fica gravado diferente do resto do arquivo, e a busca sem espaço quebraria na rodada seguinte (achado em 27/07/2026, antes de causar problema em produção). Corrigido: busca agora por regex tolerante a espaço opcional (`"id"\s*:\s*"contratos"`), e a gravação usa `separators=(',', ':')` pra manter o trecho sempre compacto, igual ao resto do arquivo. Se aparecer um bug parecido em outro módulo (busca por string JSON literal que só funciona uma vez), suspeitar do mesmo padrão: qualquer trecho que a gente reescreve com `json.dumps` sem especificar `separators=` fica com espaço, mesmo que o arquivo original seja compacto.

**Arquivo-fonte "resalvo" pode ter data interna desatualizada — sempre conferir `extraInfo.today` contra a data real, não confiar no mtime**: aconteceu em 27/07/2026 — o Luiz resalvou "Dashboard Contratos v3.4.1.html" (mtime do dia), mas o conteúdo interno (`extraInfo.today`) ainda dizia "24/07/2026" (dados de 3 dias antes, sexta-feira). Apliquei sem perceber e os números do card/módulo ficaram errados (Cartório com 12 em vez de 9, Registrado nesta data mostrando 5 em vez de 0) — só porque o arquivo em si estava desatualizado, não porque o merge tinha bug. O Luiz percebeu comparando com a tela real do sistema dele. **Regra**: antes de aplicar Contratos, sempre imprimir/checar `extraInfo.today` do arquivo-fonte e comparar com a data de hoje — se estiver defasado mais de 1 dia, avisar o Luiz ANTES de aplicar (provável export não regenerado), não só depois que ele reclamar. O Luiz reenviou o arquivo certo como "v3.4.2" e aí bateu tudo.

**`cartorio_registrado_novo` não estava sendo escrito no card do Daily Briefing pelo `--apply`** (bug irmão do de cima, achado na mesma investigação de 27/07/2026): o valor vem de `extraInfo.reg_ulysses + extraInfo.reg_eunapio`, que era só IMPRESSO como aviso no modo consulta ("...MUDOU, confirme com o Luiz...") mas nunca de fato gravado dentro de `card_obj` no `--apply` — então o card ficava travado no último valor setado manualmente (mostrou "Cartório Registrado: 5" mesmo depois de aplicar um arquivo com reg=0). Corrigido: `compute_briefing_card()` agora recebe `cartorio_registrado_novo` como parâmetro e escreve no card sempre que `extraInfo` existir na fonte.

## Campo novo `previsao_agencia` (Contratos v3.5, achado em 28/07/2026)

O arquivo-fonte "Dashboard Contratos v3.5.html" trouxe um campo novo em TODO registro de `dashboardData.contratos`: `previsao_agencia` (data prevista de entrega na agência), no mesmo padrão dos já existentes `previsao_cartorio`/`previsao_itbi`. Não é usado em nenhuma fórmula do card do Daily Briefing ainda — é só um dado extra carregado junto. **Não precisou mudar nenhum script**: `update_contratos.py` já faz `r2 = dict(r)` pra cada registro da fonte nova, então qualquer campo novo (mesmo desconhecido) é automaticamente preservado no merge e gravado em produção — só um script que fizesse `{campo1: r['campo1'], campo2: r['campo2'], ...}` explicitamente listando campos precisaria de ajuste manual quando a fonte ganha um campo novo.

**Como detectar mudança de estrutura (regra geral, útil quando o Luiz pedir pra conferir "mudança de estrutura" em algum módulo)**: comparar o UNIÃO de chaves de todos os registros da fonte nova contra o união de chaves de todos os registros já em produção (não só o 1º registro de cada — campo pode existir só em alguns registros). Script rápido:
```python
def union_keys(recs):
    s = set()
    for r in recs: s |= set(r.keys())
    return s
novos = union_keys(fonte_nova) - union_keys(producao)   # campos que apareceram
sumidos = union_keys(producao) - union_keys(fonte_nova) # campos que sumiram
```
Campo novo em TODOS os registros = estrutural (provavelmente uma coluna nova do sistema de origem). Campo que aparece só em alguns registros da fonte mas nunca existiu em produção = mesma coisa, só que nem todo registro usa (ex: contrato ainda não chegou naquela etapa). Campo que sumiu é mais grave — indica removeram uma coluna do export, ou o arquivo-fonte veio errado/truncado; **nunca aplicar um `--apply` que faria um campo sumir de produção sem confirmar com o Luiz antes**, mesmo que nenhum script quebre tecnicamente com isso.

**Quando o diff de "mudanças reais" fica poluído por um campo novo (achado em 28/07/2026)**: se um campo novo aparece em ~1000+ registros de uma vez (ex: `previsao_agencia` apareceu como `None -> 'data'` em 1.124 dos 1.148 "mudanças reais" reportadas por `update_contratos.py`), isso esconde as mudanças de negócio genuínas no meio do ruído. Antes de reportar pro Luiz, separar `real_changes` em dois grupos: registros onde o ÚNICO campo que mudou é o campo novo (ruído estrutural esperado, não precisa listar um por um) vs. registros com outros campos alterados junto (mudança de negócio de verdade — cartório→agência, itbi triagem→solicitado, situação atrasada, etc. — esses sim, listar e destacar pro Luiz).

## REGRA GERAL (27/07/2026): todo card do Daily Briefing tem que ser recalculado a cada `--apply` do módulo correspondente

O Luiz percebeu que os cards "Financeiro — A Pagar" e "Conciliação — Balanço" no Daily Briefing estavam **travados 3+ dias atrás** mesmo com os módulos Financeiro e Conciliação já tendo sido atualizados nesse meio tempo — porque `update_financeiro.py`/`update_conciliacao.py` só atualizavam TAB_METAS+rodapé, nunca o card do Daily Briefing em si (diferente de Contratos e Vendas, que já recalculavam). Ele foi direto: **"o daily de todos os modos tem que sempre mostrar a ultima atualização dos seus modulos"** — regra permanente, vale pra qualquer módulo novo que ganhar um card no Daily Briefing no futuro.

Corrigido em 27/07/2026, adicionando `find_briefing_card(t, card_id)` / `write_briefing_card(t, card_obj, bstart, bend)` em `bundler_utils.py` (busca o card pelo `id` dentro do bloco `BRIEFING_DATA`, ancorado no marcador `/* ==== Daily Briefing module ==== */`) — **todo script de update que tem um card correspondente precisa chamar isso no `--apply`**:

- **Financeiro — A Pagar** (`update_financeiro.py`): período = data mais recente presente em `DATA.pago` (não é janela, é só "o último dia que teve pagamento"), total/qtd desse dia, quebra por `Empreendimento` (não `Empresa`) em ordem decrescente.
- **Conciliação — Balanço** (`update_conciliacao.py`): período = `data_relatorio` mais recente em `RELATORIOS`; por empresa, `banco` = soma `RELATORIOS.contas.saldo`, `investido` = soma `INVESTIMENTOS.contas.saldo` (0 se a empresa não tiver), `total` = banco+investido.
- **Contratos — Gestão** e **Vendas — Unidades** já faziam isso desde antes (ver seções próprias acima).
- **Comissão — Unidades** e **Financeiro — A Receber** (seção "anterior" do Daily) ainda NÃO têm script de update automatizado nesta sessão — se o Luiz mandar fonte nova desses módulos, construir o mesmo padrão (script `update_comissoes.py`/`update_areceber.py` com recompute de card) antes de aplicar à mão.

**Bug irmão descoberto na mesma investigação**: `update_conciliacao.py` NUNCA lia nem gravava `const INVESTIMENTOS` (só cuidava de `RELATORIOS`/`PENDENCIAS`) — é uma variável JS separada dentro do módulo Conciliação, não faz parte de `RELATORIOS`. Isso significava que o saldo de investimento ficava sempre desatualizado em produção mesmo quando o arquivo-fonte já trazia dado novo (achado: arquivo trazia INVESTIMENTOS de 24/07, produção ainda mostrava 22/07). Corrigido — agora é lido/diffado/gravado como campo opcional (`load_investimentos()`, trata ausência graciosamente, igual ao `extraInfo` em Contratos).

### Cabeçalho GERAL do Daily Briefing também precisa ser atualizado (achado em 28/07/2026)

Mesmo depois da correção acima (cards individuais recalculando certinho), o Luiz ainda viu o topo da aba Daily Briefing preso em **"Atualizado: 24/07/2026 às 09:52"** enquanto os cards já mostravam datas de 27/07. Existem DOIS lugares diferentes mostrando "quando o Daily foi atualizado pela última vez", e nenhum script tocava em nenhum dos dois:

1. `TAB_METAS['briefing'].data` — o badge do topo da própria aba Daily Briefing, igual ao badge que toda outra aba tem (mesmo padrão do `patch_tab_meta`, só que ninguém nunca chamava com `'briefing'` como chave).
2. `BRIEFING_DATA.gerado_em` — alimenta a linha `"Consolidado em " + BRIEFING_DATA.gerado_em` (elemento `#briefingGen`), dentro do próprio módulo Daily Briefing.

Corrigido com a função `patch_briefing_header(t, now_str)` em `bundler_utils.py`, que atualiza os dois de uma vez — chamada em TODO `--apply` dos 4 scripts (`update_contratos.py`, `update_vendas.py`, `update_financeiro.py`, `update_conciliacao.py`), logo depois do `patch_sidebar_footer`. Mesma regra geral: **"sempre que algum modulo do daily tiver alteração, tem que colocar a data e hora da atualização mais recente"** — não é o carimbo de um card específico, é "o Daily como um todo foi tocado agora".

**Cuidado ao editar campos JSON com regex por grupo de captura**: a primeira versão de `patch_briefing_header` tinha um bug de "off-by-one" no grupo de captura da regex do `gerado_em` — o grupo 2 capturava só a ASPA de fechamento (`(")`) em vez do VALOR entre aspas (`([^"]*)`), então `t[:m.start(2)] + novo_valor + t[m.end(2):]` comia a aspa de fechamento e deixava o valor antigo colado com o novo, corrompendo o JSON (`"09:5228/07/2026 às 07:54,"janela_recente"...`, faltando aspas/vírgula). Corrigido pra capturar o VALOR como grupo separado (3 grupos: prefixo+aspa-abertura, VALOR, aspa-fechamento) e substituir só o grupo do meio — mesmo padrão já usado (corretamente) em `patch_tab_meta`. **Lição**: ao escrever uma regex de patch nova, sempre conferir com `json.loads()` o trecho afetado LOGO DEPOIS de rodar, antes de assumir que funcionou — não confiar só no fato de não ter dado exception.

### Módulo Comissões agora tem script próprio (`update_comissoes.py`, criado em 28/07/2026)

Segue o mesmo padrão dos outros (`consulta`/`--apply`, diff por multiset já que `const DADOS` não tem ID único). Card do Daily Briefing "Comissão — Unidades": acha o par `(periodo_inicio, periodo_fim)` mais recente presente em `DADOS` (maior `periodo_fim`), soma `valor_pago` desse período (`total`/`qtd`), quebra por `empreendimento` em ordem decrescente. Validado contra o valor já gravado em produção antes de aplicar qualquer mudança (período 11-17/07/2026, R$ 95.858,21, 6 registros — bateu exato). `Dashboard A Receber` (`update_areceber.py`) ainda não existe — continua manual se o Luiz mandar fonte nova desse módulo.

**Armadilha grave descoberta em 28/07/2026 (NÃO repetir)**: ao comparar um arquivo-fonte novo de Comissões contra produção, o diff deu "0 adicionados / 0 removidos" — mas era um FALSO NEGATIVO. A causa: o arquivo tinha sido lido de uma cópia local (`/tmp/out/...`) que ficou com o conteúdo de um `device_stage_files` ANTERIOR (717.440 bytes, de dias atrás), enquanto o arquivo real na pasta do Luiz já estava maior (720.519 bytes, salvo mais recentemente) — o `device_stage_files` para o MESMO nome de arquivo pode devolver um cache stale mesmo dizendo `"ok": true` com o `mtimeMs`/`bytes` corretos na resposta (a resposta da chamada bateu certo, mas o conteúdo que efetivamente apareceu em `/mnt/user-data/uploads/...` ficou desatualizado). O Luiz percebeu porque sabia que tinha dado novo: **"nao sao identicos. voce ta entendendo algo errado, eu sempre tenho que criar um arquivo novo para voce identiticar as diferenças."**

**Regra permanente pra evitar isso de novo**: depois de QUALQUER `device_stage_files`, antes de confiar no conteúdo, conferir se o tamanho do arquivo que realmente chegou em `/mnt/user-data/uploads/...` bate com o `bytes` retornado pela chamada E com o tamanho reportado por `device_list_dir` na pasta do Luiz. Se não bater (like aconteceu aqui: resposta disse 720519 mas o arquivo em disco ficou com 717440), a técnica que funcionou foi: usar `device_bash` pra copiar o arquivo original pra um NOME NOVO dentro da pasta do Luiz (via mount Linux, ex: `/sessions/<id>/mnt/financeiro + contratos/`, não o caminho Windows `C:\...` — esse só funciona pro `device_list_dir`/`device_stage_files`, não pro `device_bash`) e then dar `device_stage_files` nesse nome novo — evita qualquer cache por nome de arquivo. **Nunca reportar "sem mudanças" num diff sem essa dupla checagem de tamanho quando o resultado for suspeito (0 diffs mas o Luiz "sabe" que mandou algo novo, ou o tamanho do arquivo mudou mas o diff não achou nada).**

## Janela "recente"/"anterior" do Daily Briefing (achado e corrigido em 28/07/2026)

Existem DUAS coisas relacionadas à "janela de 2 dias" do Daily Briefing que **nenhum script nunca calculava** — ficavam travadas manualmente:

1. **`BRIEFING_DATA.janela_recente`** (texto tipo `"24/07 (hoje) e 23/07 (dia útil anterior)"`, mostrado na UI) — era um literal nunca recalculado, foi encontrado 4 dias desatualizado.
2. **Reclassificação dos cards entre `BRIEFING_DATA.recente[]` e `BRIEFING_DATA.anterior[]`** — um card cujo `data_ref` passasse a cair dentro da janela (ex: módulo atualizado HOJE) podia continuar preso no array `anterior` se já estivesse lá de uma rodada anterior; a única vez que a classificação era decidida era quando alguém organizava os arrays à mão.

O Luiz percebeu os dois de uma vez: **"Atualização Recente, que seria hoje ou dia util anterior estao com datas atrasdas, seguir as datas do dia 'hoje' e util anterior. e o comissoes estao atualizado hoje, esta com atualização anterior."**

Corrigido com duas funções novas em `bundler_utils.py`:
- `business_day_before(d)` — dia útil anterior a `d` (pula sábado/domingo; **não considera feriados**, só fim de semana — se algum feriado nacional/municipal cair numa segunda, por exemplo, a lógica ainda vai contar sexta como dia útil anterior errado; ajustar se o Luiz reclamar disso no futuro).
- `patch_janela_recente(t, hoje)` — recalcula o texto `janela_recente` E reclassifica TODO card comparando `data_ref` (`DD/MM/AAAA`) contra `{hoje, dia_util_anterior}`: dentro da janela vai pra `recente`, fora vai pra `anterior`. Preserva ordem relativa dentro de cada grupo.

Ambas chamadas de dentro de `patch_briefing_header()` (já usado pelos 4 scripts de update + `update_comissoes.py`) — **não precisa chamar separado em cada script**, já roda automaticamente em todo `--apply` que toca o Daily. `hoje` é derivado do mesmo `now_str` usado pro carimbo "Atualizado", não do relógio do sistema direto — mantém tudo consistente.

## Commit automático no git local (combinado em 30/07/2026)

O Luiz pediu pra parar de copiar o `index.txt` manualmente pro git a cada atualização: **"eu quero que voce faça o commit so, se eu precisar dar o push. eu quero que quando eu pedir 'atualizar dados' voce verifica, atualiza e commita."** Investigação feita antes de aceitar (não supor, checar):

- A pasta do Luiz (`financeiro + contratos`) É um repositório git local (branch `master`), mas **`git remote -v` está vazio — não existe remoto configurado**. Então hoje não tem "pra onde" dar push, mesmo que eu pudesse.
- `device_bash` (a ferramenta que mexe no computador do Luiz) **não tem acesso à rede** — não dá pra rodar `git push`/`fetch`/`pull` por ela mesmo que um remoto existisse.
- O `Bash` deste ambiente (nuvem) TEM rede e poderia falar com o GitHub, mas exigiria eu manusear credencial/token do Luiz — isso é proibido pela regra de segurança (nunca inserir senha/token/API key em nada, nem com autorização explícita do usuário). Por isso o `git push` em si **sempre fica com o Luiz**, não é uma limitação temporária, é permanente por essa regra.
- **Conclusão prática**: eu cuido de tudo que é local e reversível (`git add` + `git commit`, via `device_bash`, na pasta dele) — o Luiz só precisa apertar "push" (GitHub Desktop, ou `git push` no terminal, do jeito que ele já usa) quando quiser publicar.

### Como fazer (passo a passo)

1. Depois que a atualização de dados já foi entregue e conferida por md5sum (passo 10 do checklist "atualiza os dados"), rodar via `device_bash`, na pasta do Luiz:
   ```bash
   cd "caminho da pasta"
   git add "arquivo1" "arquivo2" ...   # SEMPRE listar os arquivos, nunca -A nem "git add ."
   git commit -m "mensagem descrevendo o que mudou"
   ```
2. **Nunca usar `git add -A` / `git add .`** — a pasta do Luiz tem arquivos que não são do dashboard (logos soltos, SKILL.md, backups antigos tipo "Dashboard Grupo Delta v5.3.html", e uma pasta `.git_delta/` que é OUTRO repositório git dentro da pasta, achado em 30/07/2026 e nunca investigado a fundo — não commitar nem mexer nela sem perguntar ao Luiz). Listar explicitamente só: os dois arquivos de produção (`Dashboard Grupo Delta v5.4.html`, `index.txt`), o(s) arquivo(s)-fonte do(s) módulo(s) que mudou(aram) nesta rodada, e qualquer script/documentação (`scripts/*.py`, `INSTRUCOES_ASSISTENTE.md`) que tenha sido tocado.
3. Escrever uma mensagem de commit curta em português descrevendo o que mudou de verdade (não "atualização automática" genérico) — ex: "Contratos: 2 reservas + 1 assinatura, ITBI solicitado hoje".

### Pegadinha do ambiente (achada em 30/07/2026, provavelmente vai se repetir)

O mount do computador do Luiz **não permite `unlink` de arquivo** (mesma limitação documentada em "Entrega de arquivos grandes": `device_bash` não consegue deletar nada com `rm`). Isso afeta o git de um jeito sutil: o git usa um arquivo `.git/index.lock` (e às vezes `.git/HEAD.lock`, `.git/objects/maintenance.lock`) como trava temporária durante qualquer operação que mexe no índice (`add`, `commit`, e até `status`, se ele tentar atualizar o cache de stat). Normalmente o git apaga esse arquivo de trava sozinho ao terminar — aqui ele **não consegue** (`warning: unable to unlink '.git/index.lock': Operation not permitted`), e o arquivo de trava fica órfão. Na PRÓXIMA operação git, isso quebra com `fatal: Unable to create '.git/index.lock': File exists.`

**Regra permanente**: antes de qualquer `git add`/`git commit`/`git status` via `device_bash`, checar se já existe um `.git/index.lock` órfão (`ls -la .git/index.lock`) e, se existir, mover pra `_to_delete/` (`mv .git/index.lock "_to_delete/index.lock_stale_N"` — nome novo a cada vez, mesma lógica do `_RELOAD2.html` da armadilha #18) antes de rodar o comando. Isso é esperado acontecer de novo a cada rodada de commit — não é sinal de erro nem de outro processo git rodando de verdade, é só a limitação de unlink do mount. Os warnings de `unable to unlink '.git/objects/tmp_obj_XXX'` durante o `add`/`commit` são inofensivos (mesma causa, mas não bloqueiam a operação em si — só o `index.lock` órfão bloqueia a PRÓXIMA chamada).

## Entrega de arquivos grandes (v5.4.html/index.txt, ~9 MB)

`device_commit_files` estoura timeout em arquivos desse tamanho (mesmo com retry). Fluxo validado que sempre funcionou nesta sessão:
1. Zipar os dois arquivos (`zip -j update.zip "Dashboard Grupo Delta v5.4.html" index.txt`) — cai pra ~1,9 MB por causa da repetição no texto escapado.
2. `SendUserFile` no zip.
3. `device_commit_files` do zip (pequeno, não estoura).
4. No `device_bash`, na pasta do usuário: `mkdir pasta_extract && unzip -o update.zip -d pasta_extract`, depois um Python com `shutil.copyfileobj(open(src,'rb'), open(dest,'wb'))` para regravar os arquivos finais **sem apagar** (o `device_bash` não pode deletar arquivo — `unzip -o` direto em cima do arquivo existente falha com "Operation not permitted"; `copyfileobj` trunca e regrava sem precisar de unlink).
5. Conferir `md5sum` dos arquivos finais contra os do workspace.
6. Mover o zip/pasta temporária para uma `_to_delete/` (criada na pasta do usuário) — `device_bash` não deleta arquivo nenhum, só `mv`.

## Formato técnico do arquivo (`__bundler`)

O `v5.4.html`/`index.txt` guardam o documento inteiro como uma STRING JSON dentro de `<script type="__bundler/template">...</script>`. **Isso explica por que o arquivo inteiro (não só os literais de dados) aparece com `\n` e `\"` literais em vez de quebra de linha/aspas reais** — é um efeito colateral direto desse empacotamento, não uma particularidade de cada módulo. Para editar:

```python
import json
def unpack(fname):
    c = open(fname, encoding='utf-8').read()
    tp = c.find('<script type="__bundler/template">')
    tp2 = tp + len('<script type="__bundler/template">')
    te = c.find('</script>', tp2)
    raw = c[tp2:te].strip().replace('<\\/script>', '</script>')
    return json.loads(raw), c, tp, tp2, te
```

Para salvar de volta:
```python
new_json = json.dumps(new_t, ensure_ascii=False).replace('</script>', r'<\/script>')
new_c = c[:tp] + '<script type="__bundler/template">\n' + new_json + '\n' + c[te:]
```

Se preferir fazer uma edição pontual sem desempacotar o JSON inteiro (útil em arquivo grande, ~9 MB), dá para operar direto na string bruta do arquivo, contanto que: (a) os pontos de âncora usados em `.find()` sejam substrings ASCII sem aspas nem quebra de linha (ex: `'tab-vendas'`, `'gd-v53-css'` — nunca um trecho com `"` ou `\n` reais, porque no arquivo eles aparecem escapados); e (b) qualquer conteúdo novo inserido (HTML/CSS/JS com quebras de linha e aspas) seja escapado antes com `json.dumps(texto, ensure_ascii=False)[1:-1]`, replicando a mesma convenção do resto do arquivo.

Alguns arquivos-fonte individuais (Financeiro, A Receber, Contratos) TAMBÉM usam esse mesmo formato `__bundler` — precisam ser desempacotados do mesmo jeito antes de extrair os dados. Outros (Conciliação, Análise Mensal, Comissões, Vendas) são HTML puro, sem esse empacotamento.

## Técnica de transplante seguro ("back-to-front")

Ao substituir múltiplos blocos de dados no mesmo arquivo, **sempre calcular todas as posições de corte a partir do texto ORIGINAL intacto**, nunca fazer `.replace()` sequenciais numa string que já foi alterada (os offsets mudam). Usar um parser de profundidade de chaves/colchetes (`find_matching_brace`) que respeita strings (aspas simples, duplas, backtick, escapes) para extrair literais JS que não são JSON válido (chaves sem aspas, aspas simples).

## Estrutura de cada módulo dentro do arquivo unificado

Cada módulo vive dentro de um comentário marcador, ex: `/* ==== Contratos module ==== */`, envolvido por uma IIFE:
```js
(function(){
  ... código do módulo ...
  window._mount_NOMEDOMODULO = function(){ ... };
})();
```
O sistema de abas chama `window._mount_NOMEDOMODULO()` quando o usuário clica na aba (mount preguiçoso/lazy). O HTML do módulo fica envolto em `<div id="tab-NOME" class="tab-panel" data-mounted="false"><div class="tab-panel-inner" id="inner-NOME">...</div></div>`, e o CSS é escopado com prefixo `#tab-NOME`.

Registro de um módulo novo precisa tocar em **todos** estes pontos (não só o HTML/CSS/JS do módulo em si):
- array `TABS` (ordem = ordem visual no sidebar)
- objeto `TAB_METAS` (version/data/prog)
- objeto `GD_COLOR_OFFSETS`
- string `SEL` dentro de `gd-topfix-js` (para o badge "N filtros ativos" funcionar no container de filtros do módulo)
- regra CSS `position:relative` do container de filtros (mesma lista de seletores do `SEL` acima)

### Armadilhas já identificadas e corrigidas (NÃO repetir)

1. **Colisão de nomes entre módulos**: `toggleTree`, `toggleInvDetail` e funções parecidas existem duplicadas em módulos diferentes (Conciliação e Contratos), cada uma isolada na própria IIFE. Atributos `onclick="..."` no HTML executam em escopo GLOBAL, então uma função só local não é enxergada — é preciso expor com sufixo, ex: `window.toggleTree_conciliacao = toggleTree;` e trocar o `onclick` correspondente para `onclick="toggleTree_conciliacao(this)"`. Isso vale tanto para o call-site fixo quanto para os gerados dinamicamente dentro de `render()`/templates de string — **verificar TODAS as ocorrências de `onclick="nomeFuncao("`, não só uma.**
2. **Exposição de funções fora do escopo (`window.x = x`) precisa estar DENTRO da IIFE que define `x`**, antes do `})();` de fechamento — nunca depois. Colocar depois causa `ReferenceError: x is not defined` na hora do load.
3. **Bloco de módulo duplicado/órfão**: já aconteceu do Conciliação existir DUAS vezes no arquivo — uma cópia órfã (script solto, sem IIFE, com `render()` incondicional, que é a que edições ingênuas por `.find()` acabavam sempre encontrando) e a cópia de verdade (dentro do padrão IIFE + `_mount_conciliacao`). Sempre conferir `t.count('const NOMEDAVARIAVEL')` — se aparecer mais de 1, investigar duplicidade antes de aplicar qualquer dado.
4. **CSS de módulo precisa ser escopado**: ao trazer um `<style>` novo de um arquivo-fonte standalone, ele vem com seletores globais (`:root`, `*`, `html`, `body`, `.classe`, `header` etc.). Isso PRECISA ser transformado: `:root`/`html`/`body` viram `#tab-NOMEDOMODULO{...}` (sem espaço), e qualquer outro seletor recebe o prefixo `#tab-NOMEDOMODULO ` (com espaço, seletor descendente) — inclusive dentro de blocos `@media`. Nunca copiar um `<style>` de arquivo-fonte direto sem essa transformação, ou o CSS vaza pro dashboard inteiro. **Além disso, se o `<style>` fonte redefine variáveis de tema (`--blue-900`...`--blue-50` etc.) dentro de um `:root` local, DROPAR essas redefinições** (manter só cores realmente específicas do módulo, tipo `--gold`) — senão o módulo não acompanha a troca de tema (`html[data-gdt="tN"]`) do resto do dashboard.
5. **Trocar só o `<script>` de um módulo não é suficiente quando o HTML/CSS também mudou**: o Análise Mensal tem HTML estático (ex: `<option>` de filtro) e `<style>` PRÓPRIOS, fora do bloco `<script>`. Se o arquivo-fonte mudou o corpo HTML (novo filtro, novo texto) ou cor, é preciso trocar também esse trecho de HTML/CSS, não só os dados JS.
6. **Bugs que já existiam no PRÓPRIO arquivo-fonte** (não são erro de transplante): função chamada mas nunca definida (ex: `fmtM` no lugar de `fmtK`), inconsistência de acentuação nos dados (ex: campo "Definição" com "COMISSOES" e "COMISSÕES" como valores diferentes pro filtro, causando duplicidade visual). Ao encontrar algo assim, INVESTIGAR se é bug de transplante ou bug pré-existente no arquivo-fonte antes de "corrigir" — e perguntar ao usuário como prefere tratar (normalizar no dashboard vs. corrigir na origem).
7. **Arquivo-fonte truncado**: verificar sempre `'</html>' in conteudo` antes de confiar em qualquer arquivo-fonte. Já aconteceu mais de uma vez (Contratos, Conciliação).
8. **Mount do sandbox pode ficar desatualizado** (mtime/conteúdo velho mesmo com arquivo já salvo pelo usuário). Se o usuário mostrar print com data/hora diferente do que você está vendo, ACREDITE no usuário, avise que seu acesso está desatualizado, e tente reler.
9. **Colisão de `id` de elemento entre módulos** (descoberto na integração do Comissão/Vendas — os dois foram gerados a partir do mesmo template e reusam os MESMOS ids genéricos: `cardVendaTotal`, `fEmpresa`, `chartLinha`, `treeRoot`, `btnLimpar` etc.). `getElementById` sempre resolve para o PRIMEIRO elemento com aquele id no documento — então o JS do módulo mais novo silenciosamente escreve dentro dos elementos (ocultos) do módulo mais antigo, e o próprio módulo novo fica com os campos travados em "—", sem nenhum erro no console. **Antes de inserir um módulo novo, listar TODOS os ids `id="..."` do HTML+JS do arquivo-fonte e grepar cada um contra o v5.4.html já existente; renomear qualquer um que já exista com um prefixo curto do módulo (ex: `vnd`, `cms`).** Atenção especial a **ids montados por concatenação de string em JS** (ex: `'cardVenda'+k`, `'mw-'+key`) — uma renomeação por regex `\bid\b` estático não pega esse padrão; é preciso caçar manualmente cada `getElementById(` e cada string-base usada em concatenação.

10. **Bug crítico descoberto em 28/07/2026 — corrupção silenciosa de fronteira entre módulos ao substituir o skeleton HTML (`id="inner-NOMEDOMODULO"`)**: o arquivo tem DUAS regiões separadas por módulo — (a) o **skeleton HTML estático**, todos os `<div class="tab-panel-inner" id="inner-XXX">...</div>` agrupados perto do byte ~1.5-1.6M, e (b) o **código JS do módulo**, todos os blocos `/* ==== XXX module ==== */ (function(){...})();` agrupados perto do byte ~3.4M+. NÃO ficam juntos — uma edição precisa localizar as duas regiões separadamente. Ao substituir o CONTEÚDO do skeleton HTML de um módulo (entre a abertura `<div class="tab-panel-inner" id="inner-XXX">` e o fechamento), é MUITO fácil o `.find()`/replace engolir também os 2 `</div>` de fechamento (fecha `tab-panel-inner` + fecha `tab-panel`) e a abertura COMPLETA do PRÓXIMO módulo (`<div id="tab-PROXIMO" class="tab-panel" data-mounted="...">\n  <div class="tab-panel-inner" `), deixando só um `id="inner-PROXIMO">` solto sem tag de abertura — HTML inválido, o `getElementById('tab-PROXIMO')` passa a retornar `null` e a aba fica **completamente em branco ao clicar, SEM NENHUM ERRO NO CONSOLE** (o `validate_dashboard.py`, que só verifica "0 erros de console", NÃO detecta isso — já aconteceu TRÊS vezes na mesma sessão: Conciliação→Comissão, Vendas→Contratos, e Financeiro→A Receber — essa última só foi descoberta porque o Luiz avisou depois da entrega, escapou da minha própria checagem porque "A Receber" é uma SUB-aba dentro do menu "Financeiro" e eu só conferi os módulos de nível principal). **Depois de QUALQUER edição no skeleton HTML de um módulo, sempre rodar um `regex.findall(r'id=\"tab-([a-z]+)\"')` no arquivo final e conferir que a lista tem TODOS os módulos e SUB-abas esperados** (ex: `areceber` é sub-aba de `financeiro`, não conta como coberta só por conferir `financeiro`) — se um sumiu, a fronteira foi corrompida. Além disso, tirar e OLHAR (não só gerar) screenshot de TODAS as abas E sub-abas antes de entregar, não só das que foram editadas diretamente — o bug aparece no módulo/sub-aba LOGO DEPOIS (na ordem física do arquivo) do que foi editado, então mapear a ordem física real (via os índices de `t.find(...)`) antes de decidir quais telas conferir.
11. **Antes de adotar o HTML/CSS/JS "novo" de um arquivo-fonte por inteiro, sempre checar se a versão em produção tem customizações próprias que o arquivo-fonte não tem** (ex: filtro de Status em Contratos, `datalabels:{display:false}` em 3 gráficos do Financeiro) — perguntar ao usuário quais manter antes de aplicar, nunca sobrescrever silenciosamente. Quando uma variável auxiliar usada pela customização (ex: `TEMPO_METRICAS`) está declarada com escopo local dentro de OUTRA função já existente, e uma função nova precisa dela, duplicar a declaração dentro da função nova em vez de arriscar tocar na função antiga que já funciona.
12. **CSS já escopado (`#tab-NOMEDOMODULO ...`) normalmente NÃO precisa ser retransplantado** ao adotar um novo layout de módulo — antes de mexer no `<style>`, conferir se todas as classes usadas no HTML novo já existem no CSS já escopado em produção (contar ocorrências de cada classe); se sim, não tocar no CSS. Isso preserva "mesma qualidade gráfica/efeitos" por definição, já que os efeitos vivem no CSS que não foi tocado. **Ressalva importante (achada em 28/07/2026, Comissão v1.2)**: "a classe já existe" não é garantia de que a REGRA por trás dela ainda serve pro HTML novo — a classe `.card-group` já existia no CSS de produção (regra `#tab-comissoes .card-group`), mas era herdada do layout ANTIGO (4 grupos lado a lado, cada um empilhado em coluna: `display:flex;flex-direction:column`, sem `grid-column:1/-1`). O HTML novo usa `.card-group` como UM ÚNICO bloco que deveria ocupar a largura inteira do grid com os itens em LINHA (`display:flex;flex-direction:row;grid-column:1/-1`, `.mini{flex:1}`, separador `border-left` em vez de `border-top`) — resultado: os cards renderizavam empilhados numa caixa estreita à esquerda, sobrando espaço vazio, sem nenhum erro (nem visual "quebrado" óbvio, só posição errada). **Ao reaproveitar uma classe de layout genérica que MUDOU de "múltiplas instâncias lado a lado" para "instância única ocupando a linha toda" (ou vice-versa) entre arquivo-fonte antigo e novo, comparar as regras `display`/`flex-direction`/`grid-column` da classe no fonte novo contra a versão já escopada em produção — não assumir que bateu só porque a classe já existia.**
13. **Diff de "mudança de estrutura" precisa cobrir HTML/CSS/JS, não só os dados (`const DATA`/`dashboardData`)** — comparar só o objeto de dados entre arquivo-fonte novo e produção pode dar "0 diferenças estruturais" falso-negativo quando na real o layout/labels/lógica mudaram bastante. Sempre fazer um diff separado da estrutura (HTML fora do `<script>`, JS fora do objeto de dados) além do diff de dados.

14. **Header/logo antigo pode ficar "escondido" dentro de uma aba quebrada**: a sub-aba "A Receber" tinha um `<header>` duplicado com logo/data/versão BEM antigos (v2.0, 30/06) que nunca foi limpo (regra #257) porque essa sub-aba nunca esteve no escopo dos módulos auditados — e como a aba estava com a fronteira HTML corrompida (armadilha #10), ele ficava invisível (nada renderizava). Ao consertar a fronteira, o header antigo "reapareceu" e pareceu uma regressão nova pro usuário ("o logo voltou pro antigo"), mas na verdade sempre esteve lá, só nunca tinha sido visto. Lição: sempre que corrigir uma aba que estava quebrada, revisar TAMBÉM se o conteúdo que passa a aparecer não é lixo/duplicata antiga que devia ter sido limpa (regra #257) mas nunca foi porque a aba nunca tinha sido auditada.
15. **Cuidado com o TAMANHO da região ao contar ocorrências (`t[inicio:inicio+N]`)**: já aconteceu de eu concluir errado que os gráficos de Contratos não tinham `datalabels:` porque usei uma janela de busca (`region = t[contr_start:contr_start+600000]`) menor que o módulo inteiro (o módulo Contratos tem quase 4M de caracteres) — todos os `datalabels:` estavam lá, só fora da janela que eu usei. **Sempre que for contar/verificar ausência de algo num módulo, ou usar o próximo marcador de módulo como limite (`t.find('==== Proximo module ====')`), ou não limitar `N` nenhum (ir até o fim do arquivo) — nunca chutar um tamanho de janela.**
16. **Comparar contagem de opções de `<select>` entre fonte e produção, não só os gráficos/canvas**: o filtro "Métrica" de Contratos tinha uma opção `<option value="todos" selected>Todos</option>` no v3.5 que a produção não tinha — isso muda o valor PADRÃO ao carregar a página (a visão inicial vira o gráfico combinado de 4 métricas em vez de uma métrica só), uma mudança estrutural real que só aparece comparando as opções do próprio `<select>`, não a existência dos gráficos.

17. **Toda função nova chamada via `onclick=`/`onchange=` no HTML PRECISA ser exposta em `window.NOME = NOME;`, mesmo que pareça "só mais uma função dentro da IIFE"**: ao criar a seção nova de Contratos (Análise de Tempo por Empreendimento) nesta sessão, esqueci de adicionar `window.updateBarAnalysis = updateBarAnalysis;` na lista de exposições já existente (`// Expor funções globais para onclick e __mountTab`) — resultado: `Uncaught ReferenceError: updateBarAnalysis is not defined` toda vez que o usuário trocava qualquer filtro daquela seção. Passou despercebido porque eu só testei o render INICIAL (sem interagir com os filtros) antes de entregar. **Lição dupla**: (a) sempre conferir se já existe uma lista de `window.X = X;` no módulo e adicionar a função nova ali, no mesmo padrão; (b) ao testar uma seção nova com filtros/onchange, testar de fato TROCAR um filtro via Playwright (`select_option`) antes de considerar validado — um "0 erros de console" no carregamento inicial não pega erros que só disparam com interação do usuário.
18. **O bug de cache do `device_stage_files` (documentado no item sobre Comissões) pode voltar a qualquer momento, mesmo em arquivos já visitados antes** — aconteceu de novo nesta sessão com o mesmo arquivo `Dashboard_Comissoes_v1.2.html` (o usuário atualizou o conteúdo mas manteve o mesmo nome; `device_stage_files` serviu uma cópia antiga, com o MESMO hash MD5 da vez anterior, escondendo a atualização). **Sempre que o usuário disser "atualizei o arquivo X" e o conteúdo staged parecer idêntico ao que já foi visto antes, desconfiar e comparar o MD5 do staged contra o MD5 obtido via `device_bash` direto no arquivo montado** (bypassa o cache); se divergir, copiar o arquivo pra um nome novo via `device_bash` (`cp arquivo.html arquivo_RELOAD.html`) e dar `device_stage_files` nesse nome novo — isso força uma cópia fresca. **Reforço (29/07/2026, "atualizar dados" com 4 arquivos-fonte novos ao mesmo tempo)**: o cache é bateu nos 4 de uma vez (A Receber, Contratos, Financeiro, Conciliação), então SEMPRE checar todos, não só o que "parece" mais suspeito. E o cache é **por nome de arquivo, não por conteúdo** — reusar o MESMO nome `_RELOAD.html` de uma sessão pra outra (mesmo com o arquivo de origem trocado) pode servir o conteúdo cacheado da vez anterior com aquele nome (aconteceu com Contratos: `_RELOAD.html` trouxe a versão de ONTEM, só funcionou depois de renomear pra `_RELOAD2.html`). **Sempre usar um sufixo que nunca foi usado antes na sessão** (numerar incrementalmente, ex: `_RELOAD2`, `_RELOAD3`) em vez de reciclar `_RELOAD.html` — e sempre confirmar com `md5sum` via `device_bash` direto ANTES de considerar o staged confiável, nunca assumir que funcionou só porque o nome mudou uma vez.
19. **Mudança que REMOVE informação visível (cards, gráfico, coluna) é sempre "real/ambígua" e exige confirmação antes de aplicar** — mesmo que a instrução do usuário seja genérica ("entenda as mudanças e atualize"), uma redução de detalhe (ex: Comissão v1.2 removendo os cards por empresa Viva/MRDELTA/Delta Voal e o gráfico de valores pagos à construtora) muda o que o Luiz consegue ver no dia a dia — apresentar claramente o antes/depois e perguntar se ele quer seguir o arquivo novo à risca ou manter o nível de detalhe atual antes de tocar em produção.
20. **`ChartDataLabels` (plugin do Chart.js) é registrado GLOBALMENTE pra página inteira, não por módulo**: o Contratos chama `Chart.register(ChartDataLabels)` uma vez no topo do seu script, e isso vale automaticamente pra TODO gráfico Chart.js do arquivo inteiro (é uma única SPA), não só os do próprio módulo Contratos. Ao adotar o `renderCharts` novo do Comissão (v1.2), os gráficos vieram sem nenhum bloco `datalabels:` nas `options.plugins`, porque o arquivo-fonte do Comissão nunca teve consciência desse plugin — resultado: números crus/duplicados/concatenados aparecendo por cima dos gráficos (ex: "1627828.88000000004505.63000000004"), sem nenhum erro de console. **Sempre que adotar estrutural/HTML/JS novo de QUALQUER módulo com gráficos Chart.js, checar se o Contratos (ou outro módulo que registre plugins globais) já está presente no arquivo — se sim, todo gráfico novo precisa de `plugins:{datalabels:{display:false}}` explícito dentro de `options`, a menos que o usuário peça pra mostrar os valores (nesse caso, usar o formatter customizado, não o padrão cru do plugin).** Validado visualmente via screenshot antes/depois — esse tipo de bug não aparece em `validate_dashboard.py` nem em diff de código isolado por módulo, só comparando o rendering real.
21. **Auditoria completa de todos os módulos (28/07/2026, "revisa todos os modulos")**: rodei 8 agentes em paralelo (um por módulo) comparando produção contra a fonte mais recente de cada um — achados só de "faxina técnica", nada de dado/conteúdo desatualizado. Lições novas:
    - **O badge `TAB_METAS.NOME.version` (mostrado no topo de cada aba) fica desatualizado com facilidade**: em 4 módulos (Financeiro, Contratos, Conciliação, Vendas) os DADOS já estavam sincronizados com a versão mais nova da fonte, mas o número da versão exibido no badge continuava apontando pra versão anterior (ex: mostrava "v3.4.1" com dado já sendo da v3.5). **Sempre que aplicar uma atualização de dado/estrutura de um módulo, conferir e atualizar `TAB_METAS.NOME.version` no mesmo passo** — é fácil esquecer porque não afeta o dado em si, só o texto do badge, e passa despercebido em validações automáticas.
    - **Remover um elemento HTML sem remover o CSS/JS que só existia pra ele deixa "lixo morto"**: aconteceu em A Receber (CSS do header antigo, ~1,5KB, sem elemento HTML correspondente) e em Comissão/Vendas (`.card-group.mrdelta/voal/viva::before` e `.mini.construtora .val` — resíduo do layout antigo "cards por empresa" que ambos os módulos compartilhavam de um mesmo template, removido de um mas não do outro). Não causa bug visível (zero elementos casam com o seletor), mas é peso morto no arquivo. **Depois de remover uma seção/elemento do HTML, sempre grepar as classes usadas por ele no bloco `<style>` do módulo e confirmar que nenhuma ficou órfã — e, se dois módulos compartilham histórico/template (ex: Comissão e Vendas vieram do mesmo gerador), replicar a limpeza nos dois, não só no que foi pedido.**
    - **Badge "N filtros ativos" (mecanismo `gd-topfix-js`) depende de uma lista `SEL` hardcoded de seletores `#tab-NOME .filtros`/`.filters`** — um módulo que usa a classe `.filtros` mas não foi incluído nessa lista (aconteceu com Global Mensal/Análise Mensal) nunca mostra o badge, mesmo tendo filtros ativos, sem nenhum erro. Essa lista existe em DOIS lugares que precisam ser mantidos em sincronia: o `var SEL = '...'` dentro de `<script id="gd-topfix-js">` e o seletor CSS `{position:relative}` logo acima dele (comentário "Badge contador de filtros ativos"). **Ao integrar/revisar qualquer módulo novo que tenha filtros, conferir se `#tab-NOME .filtros` (ou `.filters`, dependendo do nome de classe do módulo) está presente nas DUAS listas.**
    - **Nem todo módulo segue o padrão `/* ==== NOME module ==== */` pra localização**: o módulo Global Mensal/Análise Mensal não tem esse marcador — ele está embutido numa IIFE bem mais cedo no arquivo (não na região 2M-9M+ dos outros módulos), localizável só pelo nome de uma variável de dados única da fonte (ex: `PAGO_ALL`). Ao procurar um módulo pelo marcador padrão e não achar, tentar localizar por uma variável/const única do arquivo-fonte antes de concluir que "não existe" ou que está em outro lugar.

## Convenções de exibição

- `TAB_METAS` (dentro do JS do v5.4) guarda `version`, `data` (data/hora do PRÓPRIO módulo, refletindo o dado real) e `prog` (texto da programação de atualização) de cada aba.
- Rodapé do sidebar (`vX.X · Atualizado DD/MM/AAAA às HH:MM`) reflete a hora de SINCRONIZAÇÃO do arquivo unificado (agora, quando a IA aplica a atualização), não a data do dado em si — são conceitos diferentes, não confundir.
- Hora usada sempre em horário de Brasília: `TZ=America/Sao_Paulo date "+%d/%m/%Y às %H:%M"`.
- Título/data/versão de cada aba vêm SEMPRE do `TAB_METAS`/info-bar compartilhado — não duplicar um `<header><h1>...</h1></header>` próprio dentro do corpo do módulo (remover se o arquivo-fonte trouxer um).
- Cor do módulo: escolher um `iconColor`/`iconBg` (RGBA com alpha ~0.13) que não repita as cores já usadas pelos outros módulos no `TABS`, e um símbolo (`icon`, um único caractere/emoji) que faça sentido temático (ex: `⌂` para Vendas de imóveis, `%` para Comissão).

## Programações de atualização combinadas (editar aqui se mudar)

Atualizado em 28/07/2026 (o Luiz mudou os dias de Vendas/Comissão/A Receber/Global Mensal):

- Daily Briefing: atualizado a cada nova rodada de dados dos outros módulos
- Financeiro A Pagar/Pago: Seg-Ter-Qua-Sex (14h PGT) - Qui (16h PGT + Programação semanal)
- A Receber: quartas-feiras às 10h
- Global Mensal: quintas-feiras às 10h
- Contratos: diariamente às 14h e 18h
- Conciliação: diariamente às 14h
- Comissão: terças-feiras às 10h
- Vendas: segundas-feiras às 10h

**Cada texto de "Programação: ..." pode existir em ATÉ 3 lugares diferentes no arquivo, e todos precisam ser trocados juntos** (achado em 28/07/2026, ao mudar os 4 dias acima):
1. `TAB_METAS[chave].prog` — o badge do topo da própria aba do módulo.
2. `BRIEFING_DATA` → card do módulo (campo `"prog"`) — só existe se o módulo tiver card no Daily Briefing (contratos, vendas, financeiro, conciliacao, comissoes, areceber têm; `analisemensal` NÃO tem card, só o badge da própria aba).
3. **Só A Receber tem um TERCEIRO lugar**: um bloco HTML estático tipo cabeçalho de impressão/PDF (`<p>Programação: ...</p>`, junto com um `<p>Financeiro — A Receber</p>` / `<p>Atualizado: ...</p>` / `<p>Responsável: ...</p>` logo antes do `<div id="tab-analisemensal">`) — encontrado com valores BEM antigos (versão "v2.0", data "30/06/2026", horário "14h" em vez de "10h") que nenhum script nunca tocou. Atualizei só a linha de Programação (era o que tinha sido pedido); a versão/data desse bloco continuam desatualizadas — se o Luiz notar e quiser, dá pra atualizar também, mas não fiz isso proativamente porque não foi pedido e não sei se esse bloco é usado de verdade (parece ser só pra impressão/PDF).

Pra trocar um texto de Programação: usar `t.count(texto_antigo)` pra conferir quantas ocorrências existem ANTES de decidir a estratégia de substituição — se dois módulos diferentes acabarem tendo o mesmo texto antigo OU se o texto novo de um módulo for igual ao texto antigo de outro (rotação circular, ex: Vendas quintas→segundas ao mesmo tempo que Comissão segundas→terças), um `.replace()` sequencial simples vai colidir (o replace do primeiro módulo cria texto que bate com o padrão de busca do segundo). Usar placeholders únicos como etapa intermediária (`texto_antigo → \x00MARCADOR\x00 → texto_novo`) pra evitar isso.

## Preferências de estilo de trabalho do Luiz

- Respostas curtas e diretas, sem floreio.
- Sempre listar as etapas na aba "Progresso" (task list) antes de agir em qualquer tarefa com múltiplos passos, e ir marcando conforme avança — ele acompanha por ali, não precisa narrar tudo em texto.
- Antes de aplicar atualização de dados, sempre listar o que foi encontrado e esperar confirmação (nunca aplicar direto).
- Prefere que decisões de normalização/edição de dados (não só bug técnico) sejam perguntadas antes de aplicar.
- A pasta no computador do Luiz às vezes desconecta durante a sessão — se acontecer, avisar e seguir com o arquivo anexado diretamente no chat para não perder tempo, mas voltar a ler/gravar pela pasta assim que ela reconectar.
- Desde 30/07/2026: depois de toda entrega de "atualiza os dados" confirmada, fazer `git add` (arquivos específicos, nunca `-A`) + `git commit` local na pasta dele (ver seção "Commit automático no git local"). **Nunca dar `git push`** — fica sempre por conta do Luiz.
